import { Font20pxDirective } from './font-20px.directive';

describe('Font20pxDirective', () => {
  it('should create an instance', () => {
    const directive = new Font20pxDirective();
    expect(directive).toBeTruthy();
  });
});
